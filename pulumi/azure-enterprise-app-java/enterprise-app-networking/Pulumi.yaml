name: azure-enterprise-app-java
description: creates the required networking configuration to isolate cloud resources and handle costs
runtime: java
config:
  pulumi:tags:
    value:
      pulumi:template: java-gradle
